# Lean install set for the Vercel serverless function.
#
# NOTE: This file is installed INSTEAD of the root requirements.txt on Vercel
# via vercel.json "installCommand". The root requirements.txt pulls in
# torch/transformers and is NOT installed on Vercel (it would blow the
# serverless build-size limits). Local providers (qwen/native) are therefore
# not available on Vercel — the hosted OpenAI-compatible provider is used.
#
# IMPORTANT: trailing newline required (Vercel flag).

fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
loguru>=0.7.0
